// Skeleton (S0). Detekt + Konsist wiring, KMP source-set setFrom (R-064), and binding
// into `check` are added in S10, once the Detekt version is resolved (1.23.x can't read
// Kotlin 2.3 metadata; Detekt 2.0 is alpha — decided at S10 with fresh data).
